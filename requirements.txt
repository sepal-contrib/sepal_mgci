# These libs are requested to build common python libs
# If you are an advanced user and you are sure to not use them, you can comment the following lines
wheel
voila
earthengine-api

pysepal>=4.0.0,<5  # renamed from sepal_ui; 4.0 is the Solara/ee-client generation
pysepal-api>=0.3.0  # SepalClient: imported directly for the files API and type hints

solara
ee-client>=3.2.1  # required by pysepal 4 (provider-agnostic auth)
rasterio<1.3.11 # Leave it here even if it's already in the pysepal requirements. Check https://github.com/openforis/sepal/issues/328
uvicorn<0.30
# anyio 4.15 made the top-level imports lazy (anyio#1169); solara reads
# anyio.from_thread off a bare "import anyio" and dies at startup.
anyio<4.15

scipy

# custom libs
matplotlib
pandas
openpyxl>=3.0.3
plotly
pytest
pygaul>=0.4.2 # GAUL 2024 columns (gaul0_code); required by pysepal 4
seaborn

ipyvuetify<3
ipecharts>=1.0.8
